<view class="cu-bar tabbar bg-white">
  <view class="action study  {{active == 0 ? 'active' : ''}}" bindtap="goStudy">
    <image src="../../image/study1.png" wx:if="{{active == 1}}" />
    <image src="../../image/study2.png" wx:if="{{active == 0}}" />
    <view>我要学习</view>
  </view>
  <view class="action  person {{active == 1 ? 'active' : ''}}" bindtap="goPerson">
    <image src="../../image/person1.png" wx:if="{{active == 0}}" />
    <image src="../../image/person2.png"  wx:if="{{active == 1}}" />
    <view>我的账户</view>
  </view>
</view>
<view style="text-align:center;font-size:13px;color:#888;padding:10rpx">广东钢琴协会提供服务</view>

<login id="login" />